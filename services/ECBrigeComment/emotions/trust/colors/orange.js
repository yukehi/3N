exports.orange = (userEmotion, userColor,postEmotion, postColor) => { 
  if(postEmotion === 'Joy' && postColor ==='yellow'){
    return{emotion:'Angry', color:'pink'};
  }
  if(postEmotion === 'Joy' && postColor ==='pink'){
    return{emotion:'Angry', color:'green'};
  }
  if(postEmotion === 'Joy' && postColor ==='orange'){
    return{emotion:'Angry', color:'yellow'};
  }
  if(postEmotion === 'Joy' && postColor ==='lime'){
    return{emotion:'Angry', color:'orange'};
  }
  if(postEmotion === 'Joy' && postColor ==='aqua'){
    return{emotion:'Angry', color:'aqua'};
  }
  if(postEmotion === 'Joy' && postColor ==='green'){
    return{emotion:'Angry', color:'lime'};
  }
  if(postEmotion === 'Joy' && postColor ==='blue'){
    return{emotion:'Angry', color:'red'};
  }
  if(postEmotion === 'Joy' && postColor ==='red'){
    return{emotion:'Angry', color:'blue'};
  }
  /// Angry
  if(postEmotion === 'Angry' && postColor ==='yellow'){
    return{emotion:'Joy', color:'pink'};
  }
  if(postEmotion === 'Angry' && postColor ==='pink'){
    return{emotion:'Joy', color:'green'};
  }
  if(postEmotion === 'Angry' && postColor ==='orange'){
    return{emotion:'Joy', color:'yellow'};
  }
  if(postEmotion === 'Angry' && postColor ==='lime'){
    return{emotion:'Joy', color:'orange'};
  }
  if(postEmotion === 'Angry' && postColor ==='aqua'){
    return{emotion:'Joy', color:'aqua'};
  }
  if(postEmotion === 'Angry' && postColor ==='green'){
    return{emotion:'Joy', color:'blue'};
  }
  if(postEmotion === 'Angry' && postColor ==='blue'){
    return{emotion:'Joy', color:'green'};
  }
  if(postEmotion === 'Angry' && postColor ==='red'){
    return{emotion:'Joy', color:'red'};
  }
  ///Trust
  if(postEmotion === 'Trust' && postColor ==='yellow'){
    return{emotion:'Trust', color:'pink'};
  }
  if(postEmotion === 'Trust' && postColor ==='pink'){
    return{emotion:'Trust', color:'lime'};
  }
  if(postEmotion === 'Trust' && postColor ==='orange'){
    return{emotion:'Trust', color:'yellow'};
  }
  if(postEmotion === 'Trust' && postColor ==='lime'){
    return{emotion:'Trust', color:'orange'};
  }
  if(postEmotion === 'Trust' && postColor ==='aqua'){
    return{emotion:'Trust', color:'aqua'};
  }
  if(postEmotion === 'Trust' && postColor ==='green'){
    return{emotion:'Trust', color:'blue'};
  }
  if(postEmotion === 'Trust' && postColor ==='blue'){
    return{emotion:'Trust', color:'green'};
  }
  if(postEmotion === 'Trust' && postColor ==='red'){
    return{emotion:'Trust', color:'red'};
  }
  ///Surprised
  if(postEmotion === 'Surprised' && postColor ==='yellow'){
    return{emotion:'Feared', color:'pink'};
  }
  if(postEmotion === 'Surprised' && postColor ==='pink'){
    return{emotion:'Feared', color:'lime'};
  }
  if(postEmotion === 'Surprised' && postColor ==='orange'){
    return{emotion:'Feared', color:'yellow'};
  }
  if(postEmotion === 'Surprised' && postColor ==='lime'){
    return{emotion:'Feared', color:'orange'};
  }
  if(postEmotion === 'Surprised' && postColor ==='aqua'){
    return{emotion:'Feared', color:'aqua'};
  }
  if(postEmotion === 'Surprised' && postColor ==='green'){
    return{emotion:'Feared', color:'blue'};
  }
  if(postEmotion === 'Surprised' && postColor ==='blue'){
    return{emotion:'Feared', color:'green'};
  }
  if(postEmotion === 'Surprised' && postColor ==='red'){
    return{emotion:'Feared', color:'red'};
  }
  ///Feared
  
  if(postEmotion === 'Feared' && postColor ==='yellow'){
    return{emotion:'Anticipated', color:'pink'};
  }
  if(postEmotion === 'Feared' && postColor ==='pink'){
    return{emotion:'Anticipated', color:'lime'};
  }
  if(postEmotion === 'Feared' && postColor ==='orange'){
    return{emotion:'Anticipated', color:'yellow'};
  }
  if(postEmotion === 'Feared' && postColor ==='lime'){
    return{emotion:'Anticipated', color:'orange'};
  }
  if(postEmotion === 'Feared' && postColor ==='aqua'){
    return{emotion:'Anticipated', color:'aqua'};
  }
  if(postEmotion === 'Feared' && postColor ==='green'){
    return{emotion:'Anticipated', color:'blue'};
  }
  if(postEmotion === 'Feared' && postColor ==='blue'){
    return{emotion:'Anticipated', color:'green'};
  }
  if(postEmotion === 'Feared' && postColor ==='red'){
    return{emotion:'Anticipated', color:'red'};
  }
  ///Sad
  if(postEmotion === 'Sad' && postColor ==='yellow'){
    return{emotion:'Joy', color:'pink'};
  }
  if(postEmotion === 'Sad' && postColor ==='pink'){
    return{emotion:'Joy', color:'lime'};
  }
  if(postEmotion === 'Sad' && postColor ==='orange'){
    return{emotion:'Joy', color:'yellow'};
  }
  if(postEmotion === 'Sad' && postColor ==='lime'){
    return{emotion:'Joy', color:'orange'};
  }
  if(postEmotion === 'Sad' && postColor ==='aqua'){
    return{emotion:'Joy', color:'aqua'};
  }
  if(postEmotion === 'Sad' && postColor ==='green'){
    return{emotion:'Joy', color:'blue'};
  }
  if(postEmotion === 'Sad' && postColor ==='blue'){
    return{emotion:'Joy', color:'green'};
  }
  if(postEmotion === 'Sad' && postColor ==='red'){
    return{emotion:'Joy', color:'red'};
  }
  ///Anticipated
  if(postEmotion === 'Anticipated' && postColor ==='yellow'){
    return{emotion:'Joy', color:'pink'};
  }
  if(postEmotion === 'Anticipated' && postColor ==='pink'){
    return{emotion:'Joy', color:'lime'};
  }
  if(postEmotion === 'Anticipated' && postColor ==='orange'){
    return{emotion:'Joy', color:'yellow'};
  }
  if(postEmotion === 'Anticipated' && postColor ==='lime'){
    return{emotion:'Joy', color:'orange'};
  }
  if(postEmotion === 'Anticipated' && postColor ==='aqua'){
    return{emotion:'Joy', color:'aqua'};
  }
  if(postEmotion === 'Anticipated' && postColor ==='green'){
    return{emotion:'Joy', color:'blue'};
  }
  if(postEmotion === 'Anticipated' && postColor ==='blue'){
    return{emotion:'Joy', color:'green'};
  }
  if(postEmotion === 'Anticipated' && postColor ==='red'){
    return{emotion:'Joy', color:'red'};
  }

  //Disgust 
  if(postEmotion === 'Disgust' && postColor ==='yellow'){
    return{emotion:'Joy', color:'pink'};
  }
  if(postEmotion === 'Disgust' && postColor ==='pink'){
    return{emotion:'Joy', color:'lime'};
  }
  if(postEmotion === 'Disgust' && postColor ==='orange'){
    return{emotion:'Joy', color:'yellow'};
  }
  if(postEmotion === 'Disgust' && postColor ==='lime'){
    return{emotion:'Joy', color:'orange'};
  }
  if(postEmotion === 'Disgust' && postColor ==='aqua'){
    return{emotion:'Joy', color:'aqua'};
  }
  if(postEmotion === 'Disgust' && postColor ==='green'){
    return{emotion:'Joy', color:'blue'};
  }
  if(postEmotion === 'Disgust' && postColor ==='blue'){
    return{emotion:'Joy', color:'green'};
  }
  if(postEmotion === 'Disgust' && postColor ==='red'){
    return{emotion:'Joy', color:'red'};
  }

}